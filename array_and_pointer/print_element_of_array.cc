#include "array_funcs.h"
#include <string>
using namespace std ;

//int main() {
//	const size_t arr_sz = 3 ;
//	int ia[arr_sz] = {0,1,2} ;
//	print_element_of_array( ia, 3 ) ;
//
//	string sa[arr_sz] = {"a","b","c"} ;
//	print_element_of_array( sa, 3 ) ;
//
//	return 0 ;

//    const size_t arr_sz = 5;
//    int int_arr[arr_sz] = { 0, 1, 2, 3, 4 };
//    // pbegin points to first element, pend points just after the last
//    for (int *pbegin = int_arr, *pend = int_arr + arr_sz;
//              pbegin != pend; ++pbegin)
//        cout << *pbegin << ' '; // print the current element

//}
